{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd ../database && python generate_db.py && cp graph.db ../backend/"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT server:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
