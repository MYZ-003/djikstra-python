{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m pip install -r backend/requirements.txt && cd database && python generate_db.py && cp graph.db ../backend/"
  },
  "deploy": {
    "startCommand": "cd backend && python -m gunicorn --bind 0.0.0.0:$PORT server:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
